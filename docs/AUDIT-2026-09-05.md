# Аудит GPO — 5 сентября 2026

**Вывод: у проекта содержательная инженерная база, но текущая версия не готова к стабильной демонстрации полного сценария и эксплуатации.** Основной сценарий «поставить точки → построить → отправить» сейчас сломан на уровне сборки зависимостей React-хуков. После его исправления останутся независимые дефекты native solver, обмена с контроллером и энергетической модели.

Аудит выполнен лично основным агентом. После просьбы работать без субагентов помощники остановлены; их результаты в отчёте не использованы. Код приложения не исправлялся.

## Объём и достоверность

Исходная ревизия: `418b1e8` (`fix: start Webots without stale runtime zones`), рабочее дерево перед аудитом чистое. Инвентаризация: 578 отслеживаемых файлов; около 33 тысяч строк JS/C/C++/заголовков вместе с тестами. Это существенно больше, чем просто Vite-фронтенд.

Рассмотрены все основные подсистемы: React-композиция и редакторы, геометрия и маршрутизация, энергия и зарядки, импорт/экспорт, телеметрия, shared-контракты, HTTP/WebSocket bridge, файловый обмен, C++ solver, жизненный цикл и навигация C-контроллера, сенсоры/карты, сборка, Docker и тесты. Приоритет ручного чтения — точки входа, границы подсистем, критические алгоритмы и обработка отказов. Это аудит подсистем и основных потоков, а не утверждение о формальной проверке каждой строки или отсутствии других ошибок.

Проверки выполнены на Windows, Node.js 22.19.0, MSVC и установленном Webots SDK. Браузер проверен на 1280×720 и 390×844. Его соединения перехвачены, чтобы действия аудита не записывали команды в текущую сцену; эти проверки не считаются сквозными тестами робота. Диагностические проверки bridge использовали отдельный сервер на случайном loopback-порту и тестовое хранилище. C-проверки использовали отдельные временные файлы.

## Результаты запуска

| Проверка | Результат | Что это подтверждает |
|---|---|---|
| `npm run lint` | Успешно | Текущие правила ESLint соблюдены |
| `npm run build` | Успешно | Обычная production-сборка фронтенда работает |
| `npm run test` | 80 файлов, 200 тестов прошли | Существующий набор Vitest зелёный |
| `npm run native:test` | 7 C++ тестовых программ прошли | Native solver собран, существующие проверки прошли |
| `npm run test:webots` | 85 C тестовых программ прошли | Модули контроллера проходят автономные проверки |
| `npm run webots:build` | Успешно | Production-контроллер компилируется и линкуется |
| `npm run test:bridge` | Успешно | HTTP health и расчёт TSP на трёх точках работают |
| `npm audit --json` | 0 известных уязвимостей | Реестр не сообщил уязвимостей для проверенного lock-файла |
| Нажатия «Построить» и «Отправить» в браузере | Ошибки JavaScript | Основной UI-сценарий не работает |
| Сборка с набором файлов из frontend Dockerfile | Ошибка разрешения `shared/contracts` | В Dockerfile отсутствуют необходимые исходники |
| Native TSP на двух точках | 3 алгоритма аварийно завершились | Граничный допустимый ввод не обработан |

Docker daemon недоступен: отсутствует `dockerDesktopLinuxEngine`. Реальная сборка контейнеров и Compose не проверены; дефект состава frontend-образа воспроизведён обычным Vite build в отдельной копии его исходников. Проверка полной физической симуляции Webots, длительные прогоны, Linux-сборка и измерение покрытия тестами не выполнены. `npm audit` не является аудитом безопасности собственной логики.

В ходе аудита также ошибочно пробовался `node --test bridge/**/*.test.mjs`: он неприменим, поскольку эти файлы используют Vitest. Ошибки этого запуска не считаются дефектами проекта; штатный `npm run test` прошёл.

## Подтверждённые проблемы

Приоритет P1 — исправить до следующей полноценной демонстрации; P2 — устранить до устойчивой эксплуатации. Нумерация нужна для дальнейшего отслеживания.

### 1. P1 — «Построить» и «Отправить» падают из-за отсутствующих аргументов

[useDashboardPlannerActions.js:9](C:/GPO_nimmwee/src/features/planner/hooks/useDashboardPlannerActions.js:9) собирает параметры для `useDashboardPlannerRouteLifecycle`, но не передаёт `plannerModel`, `optimizedRoute`, `isOptimizing`. Первый нужен оптимизации и отправке, второй — отправке, третий — защите от повторного запуска.

В браузере «Построить» вызывает `Cannot read properties of undefined (reading 'visitPoints')` в [usePlannerRouteOptimization.js:22](C:/GPO_nimmwee/src/features/planner/hooks/usePlannerRouteOptimization.js:22). «Отправить» вызывает аналогичную ошибку чтения `length` в [usePlannerRouteSender.js:32](C:/GPO_nimmwee/src/features/planner/hooks/usePlannerRouteSender.js:32). Сеть и наличие solver на эти ошибки не влияют.

Исправление: восстановить передачу всех обязательных зависимостей и добавить интеграционный тест собранного Dashboard с реальными обработчиками кнопок. Типизация или проверяемые JSDoc-контракты помогли бы ловить этот класс ошибок до браузера.

### 2. P1 — native solver обращается за границы массива на двух точках

[tour_operations.cpp:98](C:/GPO_nimmwee/native/src/tour_operations.cpp:98), [tour_operations.cpp:122](C:/GPO_nimmwee/native/src/tour_operations.cpp:122), [ga_tabu.cpp:66](C:/GPO_nimmwee/native/src/ga_tabu.cpp:66).

При длине порядка 2 код выбирает `left=1`, затем `right=2`: `uniform_int` возвращает нижнюю границу даже при перевёрнутом диапазоне. Следующая перестановка выходит за границы вектора.

Проверен TSP `(0,0), (1,0)`, seed 1337, стандартные параметры: `ga_tabu`, `otshig`, `cuckoo` завершаются с ошибкой; `rasseivanie` возвращает ожидаемый цикл длиной 2. Для GA код процесса `3221225477 / 0xC0000005` — access violation. При этом интерфейс считает две точки достаточными.

Исправление: прямое решение для малого числа точек и защита операций перестановки; регрессии на 0/1/2/3 точках для всех алгоритмов. Текущий [route_solver_service_test.cpp:5](C:/GPO_nimmwee/native/tests/route_solver_service_test.cpp:5) проверяет лишь пустой и одноточечный случаи.

### 3. P1 — frontend Dockerfile не содержит необходимых файлов

[frontend.Dockerfile:8](C:/GPO_nimmwee/docker/frontend.Dockerfile:8) копирует `src`, `index.html`, `vite.config.js`, но не `shared`. Фронтенд импортирует оттуда контракты и координатную схему. Воспроизведена ошибка `Could not resolve "../../shared/contracts/index.js"`.

Также не копируются `postcss.config.js` и `tailwind.config.js`: после устранения ошибки импортов необходимо проверить обработку Tailwind в самом контейнере, а не только успешный выход сборки. Исправление: полный набор build inputs и проверка готовой страницы из контейнера.

### 4. P1 — быстрые изменения маршрутов и команд теряются

[controller_io.c:18](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_io.c:18) возвращает `st_mtime` в секундах. [controller_route_zone_service.c:92](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_route_zone_service.c:92) пропускает чтение, если эта величина не изменилась. Та же схема применяется к runtime-командам в [controller_runtime_command_reload_service.c:38](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_runtime_command_reload_service.c:38).

Отдельная C-проверка: после чтения первого маршрута записан другой с той же секундной отметкой; `route_changed=0`, статус `UNCHANGED`. Поскольку файл впоследствии не меняется, простое ожидание не доставляет пропущенную команду.

Исправление: явная версия/идентификатор команды и подтверждение применения; как минимум точный timestamp или контроль содержимого с повтором после ошибки чтения.

### 5. P1 — bridge и контроллер допускают разные размеры данных

[route-validation.cjs:2](C:/GPO_nimmwee/bridge/protocol/route-validation.cjs:2) допускает 1000 точек. В [controller_types.h:4](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_types.h:4) пределы — 768 waypoint, 32 зоны, 48 вершин на зону. Загрузчики не отклоняют превышение, а обрезают данные.

Воспроизведено: 800 точек → статус успешной загрузки и 768 точек; 33 зоны → успешная загрузка и 32 зоны. Аналогичная ветка обрезает вершины полигона. Это изменяет исполняемый маршрут и геометрию ограничений относительно UI.

Исправление: единые лимиты в shared-контракте, проверка после всех обходов/зарядок, явное отклонение превышений контроллером. Учитывать замыкание TSP и дополнительные обходные точки.

### 6. P2 — повторная отправка идентичного маршрута не запускает его заново

[controller_route_zone_service.c:99](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_route_zone_service.c:99) считает маршрут новым только при отличии координат/heading. Воспроизведено: тот же CSV с новой секундной отметкой прочитан успешно, но `route_changed=0`.

Сброс `route_finished` и waypoint index выполняется только при принятии изменённого маршрута в [controller_route_zone_reload_service.c:86](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_route_zone_reload_service.c:86). Поэтому явный повторный запуск после завершения не отличается от обновления состояния. Исправление: отделить «установить маршрут» от «начать выполнение», сохранять идентификатор запуска.

### 7. P1 — WebSocket принимает команды с произвольным Origin

[route-server.cjs:19](C:/GPO_nimmwee/bridge/servers/route-server.cjs:19): нет проверки Origin и аутентификации, роль определяется строкой URL. Изолированное подключение с `Origin: https://untrusted.example` принято как UI-клиент. Сервер способен принять изменяющую состояние команду от такого клиента.

Loopback и loopback-публикация Docker-портов — полезные ограничения, но не авторизация. Возможность обращения из конкретного браузера дополнительно зависит от его политики доступа к локальной сети; сам сервер её не обеспечивает. Аналогично стоит ограничить доступ к telemetry и wildcard CORS solver API.

Исправление для локального стенда: список разрешённых Origin, session token, различение прав UI и контроллера. Публичное сетевое размещение до этого не готово.

### 8. P2 — невалидная команда всё равно пересылается контроллеру, UI не получает отказ

[route-server.cjs:40](C:/GPO_nimmwee/bridge/servers/route-server.cjs:40): исключение в `routeService.handle` лишь логируется; выполнение доходит до `controllerConnection.send`. Проверка с тестовым хранилищем, отклоняющим валидацию: маршрут с `x:"invalid"` всё равно получен WS-клиентом контроллера.

Текущий Webots-путь использует файлы, поэтому это не доказательство исполнения невалидного WS-маршрута именно C-контроллером. Но серверный контракт нарушен, а при ошибке записи файлов UI тоже не получает отказ. [usePlannerRouteSender.js:79](C:/GPO_nimmwee/src/features/planner/hooks/usePlannerRouteSender.js:79) сообщает успех и запускает таймер сразу после `socket.send`, без подтверждения записи или принятия роботом.

Исправление: прекращать обработку при ошибке; ACK/NACK по requestId; отдельные состояния «отправлено», «сохранено», «принято», «выполняется».

### 9. P2 — план зарядок допускает недостаточный запас энергии

[chargingPlannerLegs.js:49](C:/GPO_nimmwee/src/features/planner/model/chargingPlannerLegs.js:49) оценивает энергию каждого ребра отдельно; поворот между рёбрами не вычитается из остатка. Итоговая оценка [chargingPlanner.js:103](C:/GPO_nimmwee/src/lib/chargingPlanner.js:103) добавляет повороты уже после выбора допустимого плана.

Воспроизведено без станций и покрытий: маршрут `(0,0) → (1,0) → (1,1)`, батарея 2.2. Результат: `ok:true`, `stationStopCount:0`, `routeEnergy:2.5026548245743667`. Даже собственная модель проекта считает расход выше запаса.

Исправление: учитывать направление входа и поворот при переходе; проверять энергию каждого участка между зарядками после сборки маршрута.

### 10. P2 — расход покрытия определяется только серединой сегмента

[energyEstimator.js:89](C:/GPO_nimmwee/src/features/planner/model/energyEstimator.js:89) относит весь сегмент к покрытию его средней точки. Узкое покрытие можно пересечь и не учесть вовсе.

Воспроизведено: путь `(0,0) → (10,0)`, rough-полоса `x∈[0,2], y∈[-1,1]`. Энергия одним сегментом — 10; тот же геометрический путь с промежуточной точкой `(2,0)` — 10.920589. Разница вызвана только представлением пути.

Исправление: разбиение по пересечениям границ покрытий либо численное интегрирование с контролируемым шагом. Проверять инвариантность энергии к вставке коллинеарных точек.

## Риски, установленные чтением кода

Следующие сценарии не прогонялись полностью с роботом; часть UI-рисков сейчас закрыта более ранним дефектом №1.

### 11. P1 — файловые команды не публикуются атомарно

[web-state-store.cjs:73](C:/GPO_nimmwee/bridge/artifacts/web-state-store.cjs:73) перезаписывает рабочие CSV/TXT напрямую. Обработчики WS асинхронны и не сериализуют последовательные сообщения. Контроллер параллельно открывает эти файлы, а отметку `last_checked` сохраняет до успешного разбора.

Возможны чтение пустого/частичного файла, принятие укороченного CSV, временная очистка зон и несогласованность route/motion. Потеря повторного чтения усугубляется дефектом №4. Исправление: очередь записи, временный файл и атомарная замена, согласованная версия набора данных; при ошибке разбора сохранять последнюю корректную версию и повторять чтение. Частота гонки под нагрузкой не измерялась.

### 12. P2 — reconnect восстанавливает сокет, но не состояние

[usePlannerBridgeSync.js:14](C:/GPO_nimmwee/src/features/planner/hooks/usePlannerBridgeSync.js:14) отправляет зоны только при изменении payload. `connected`/поколение соединения не является зависимостью. Неуспешная временная отправка не хранится в очереди. Если править зоны при выключенном bridge, после его запуска новые зоны автоматически не повторяются.

Отдельно [controller_app_lifecycle.c:269](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_app_lifecycle.c:269) использует `ignore_existing`: после перезапуска контроллера прежние файлы зон намеренно не загружаются. В сочетании с отсутствием повторной синхронизации UI и сцена могут разойтись. Исправление: handshake со снимком актуального состояния и версией сессии контроллера.

### 13. P2 — редактирование плана может само отправить маршрут

[usePlannerRouteRebuild.js:79](C:/GPO_nimmwee/src/features/planner/hooks/usePlannerRouteRebuild.js:79) реагирует на зоны, станции, покрытия и запас энергии и вызывает отправку маршрута при наличии `routeSeed`. Нет признака, что пользователь уже разрешил исполнение именно этого плана. После исправления №1 изменение покрытия/станции у построенного, но ещё не отправленного маршрута может начать исполнение; изменение после финиша может запустить его снова.

Исправление: отдельное состояние активной миссии; автоперестройку отправлять только для активного исполнения. Для черновика изменять только предпросмотр.

### 14. P2 — результат оптимизации может вернуть устаревшие точки

[usePlannerRouteOptimization.js:30](C:/GPO_nimmwee/src/features/planner/hooks/usePlannerRouteOptimization.js:30) ожидает fetch и затем без проверки актуальности сохраняет результат. Очистка/изменение точек и параметров не отменяет запрос и не увеличивает проверяемую версию плана. После исправления №1 запоздавший ответ сможет восстановить маршрут из уже удалённых точек.

Исправление: revision token/AbortController, таймаут запроса, проверка актуальности перед всеми setState. Попытка браузерной проверки этого сценария остановилась на подтверждённом дефекте №1, поэтому здесь именно вывод по коду.

## Архитектура, модель и качество продукта

**Сильные стороны.** Есть реальная функциональная декомпозиция: UI, модели, транспорт, файловое хранилище и native solver разделены; C-контроллер имеет тестируемые модули навигации, геометрии и сенсоров. Координатный контракт выделен. Solver запускается отдельным процессом с таймаутом 30 секунд и ограничением двух параллельных расчётов. Есть лимит HTTP-body и валидация чисел. JSON/Excel импорт отделён от интерфейса. Dashboard и SheetJS загружаются лениво: текущие gzip-размеры примерно 75.5 КБ основной JS, 37.6 КБ Dashboard, 163 КБ SheetJS. Это разумное разделение нагрузки.

**Избыточное число промежуточных обёрток.** В UI множество хуков только перекладывают один большой набор зависимостей в другой. Дефект №1 находится именно на такой границе. В C есть аналогичные слои service/runtime/adapter; сами по себе они допустимы, но их польза должна измеряться ясностью владения состоянием, а не числом маленьких файлов. Важнее добавить контрактные проверки, чем продолжать дробление.

**Тестов много, но уверенность не соответствует их числу.** Компонентные тесты девяти sidebar-секций используют `renderToStaticMarkup`: проверяют текст и HTML, а не клики и эффекты. Архитектурные проверки часто ищут строки импортов и имена файлов. Они не поймали отсутствие аргументов основного хука и неполный Dockerfile. Bridge smoke проверяет health и одну задачу на трёх точках, не маршрут UI → файл → controller → подтверждение. Native service test не проверяет двухточечный случай. CI-конфигурация в отслеживаемых файлах отсутствует. Нужны сценарные тесты, а не просто увеличение общего счётчика.

**Оптимальность маршрута ограничена.** C++ оптимизирует евклидовы расстояния; препятствия и зарядки добавляются позднее в JS. Это может давать допустимый обход, но не гарантирует хороший порядок посещения для итоговой стоимости с зонами и энергией. Эвристики не обязаны выдавать глобальный оптимум. Для оценки «насколько хорош» нужен воспроизводимый набор задач, эталон для малых размеров, несколько seed и метрики итогового пути, времени расчёта и допустимости. Сейчас таких результатов в репозитории не представлено.

**Энергия и покрытия пока являются моделью планирования.** Передаваемый маршрут содержит координаты, но не операции зарядки/время зарядки; физическая зарядка и состояние батареи не входят в проверенный контур исполнения. Покрытия в [controller_webots_simulation_format.c:85](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_webots_simulation_format.c:85) создаются как визуальные `Pose/Shape`; сам этот код не задаёт им различное трение. [controller_motion_profile.c:66](C:/GPO_nimmwee/webots/controllers/youbot_web/controller_motion_profile.c:66) ограничивает линейную скорость по общему профилю, не по текущему полигону покрытия. Поэтому UI-значения «факт»/«лимит по покрытию» нельзя считать измеренной физикой робота. Для исследовательского стенда допустимо, если явно назвать допущения и калибровать модель.

**Эксплуатация и наблюдаемость.** WebSocket connected отражает соединение с bridge, а не свежесть данных робота. Нужны возраст последней телеметрии, идентификатор активного маршрута и понятный отказ контроллера. Таймер UI использует `Date.now`, тогда как headless Webots настроен на `--mode=fast`; сравнение его с плановым временем смешивает реальное и симуляционное время. Для режимов исполнения нужны явные остановка/пауза/возобновление; текущая очистка точек не является остановкой робота.

**Интерфейс.** На настольном экране карта рисуется, точки добавляются/очищаются, панели визуально последовательны. Но обе панели занимают большую часть ширины, основная кнопка запуска требует прокрутки, подписи карты мелкие. При 390 px фиксированные панели шириной 310+330 px практически вытесняют карту; мобильный режим неудобен. Canvas управляется мышью и не имеет полноценного клавиатурного/доступного редактора. Стоит определить минимальную поддерживаемую ширину либо добавить адаптивные панели. [Desktop](C:/GPO_nimmwee/docs/audit-2026-09-05/desktop.png), [Mobile](C:/GPO_nimmwee/docs/audit-2026-09-05/mobile.png).

**Сохранение работы.** Полный план живёт преимущественно в React state; localStorage сохраняет скорость и панели. Перезагрузка теряет точки и зоны, а начальная синхронизация может переписать состояние bridge пустыми зонами. Импорт графа и PNG-экспорт карты не заменяют сохранение/восстановление проекта. Это существенно для регулярного использования.

**Документация.** README полезнее AGENTS.md: последний ошибочно утверждает, что нет тестового runner и Git-истории. README по-прежнему рекомендует полный Docker-сценарий, который блокируется №3. Нужны актуальный список ограничений, описание реального файлового транспорта Webots и воспроизводимый запуск с проверкой готовности каждого компонента.

## Оценка готовности и порядок работ

| Область | Оценка |
|---|---|
| Инженерная база и объём реализации | Хорошая основа для учебного/исследовательского стенда |
| Основной сценарий пользователя | Не готов: два действия падают до сетевого запроса |
| Надёжность обмена | Недостаточна: нет подтверждённых версий команд, есть потери и обрезания |
| Расчёты и физическая достоверность | Есть полезная модель, но найдены ошибки и не подтверждена калибровка |
| Развёртывание | Локальные сборки работают; Docker требует исправления и проверки |
| Тестирование | Сильная начальная база модульных тестов, слабая проверка интеграции |
| Готовность к внешнему использованию | Не готов |

Процент готовности без согласованного перечня требований был бы выдуманным. Практический статус — **развиваемый прототип с блокирующими регрессиями**, который имеет смысл доводить, а не переписывать целиком.

Порядок исправлений:

1. Восстановить основной UI-поток (№1), добавить настоящий сценарный тест, закрыть двухточечный native crash (№2).
2. Исправить Docker inputs (№3) и реально собрать/открыть контейнерный фронтенд.
3. Сделать надёжный протокол исполнения: единые лимиты, атомарная запись, версии команд, ACK/NACK, повторная синхронизация и явный повторный запуск (№4–8, №11–13).
4. Исправить энергетический расчёт и устаревшие async-результаты (№9–10, №14); проверить базовые маршруты, пограничные запасы, покрытия и зарядки.
5. Провести сквозные Webots-прогоны: обычный маршрут, повторный запуск, препятствие, смена зон, недостижимая цель, перезапуск bridge/контроллера, отключение телеметрии. Проверять фактический результат по идентификатору миссии.
6. Закрепить проверки в CI, затем улучшать сохранение проектов, UX, адаптивность и метрики качества алгоритмов.

Критерий готовности к демонстрации: на чистом запуске пользователь проходит основной сценарий без консольных ошибок, контроллер подтверждает именно отправленную версию и завершает маршрут; перезапуск и повторная отправка ведут себя предсказуемо. Сейчас этот критерий не выполнен.

## Артефакты аудита

[Результаты диагностик](C:/GPO_nimmwee/docs/audit-2026-09-05/verification.txt) и [ошибки браузера](C:/GPO_nimmwee/docs/audit-2026-09-05/browser-errors.txt) сохранены рядом со снимками. Отслеживаемые исходники не изменены; сборки обновили локальные генерируемые бинарники. Временные сервер и браузер аудита остановлены.

Автоматическая проверка безопасности отклонила команду удаления диагностических каталогов (`blocked by policy`), поэтому `C:/GPO_nimmwee/tmp/audit-20260905` и `C:/GPO_nimmwee/.playwright-cli` остались. В первом находится, в частности, копия `src` для проверки Dockerfile: до удаления/переноса этой временной копии повторный глобальный поиск тестов может обнаруживать её тесты дополнительно. Результат 80 файлов / 200 тестов относится к запуску до создания этой копии.
